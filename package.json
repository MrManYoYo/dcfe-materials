{
  "name": "@dc-tech/ice-vue-material-template",
  "version": "0.4.0",
  "description": "ice dc-tech materials",
  "author": "MrManYoYo",
  "license": "MIT",
  "scripts": {
    "deploy": "npm run generate && npm run sync",
    "generate": "iceworks generate",
    "sync": "iceworks sync",
    "lint": "npm run eslint && npm run stylelint",
    "eslint": "eslint --cache --ext .js,.jsx ./",
    "stylelint": "stylelint ./**/*.scss",
    "prepublishOnly": "npm run generate"
  },
  "files": [
    "src/",
    "lib/",
    "build/"
  ],
  "devDependencies": {
    "@ice/screenshot": "^0.1.1",
    "@ice/spec": "^1.0.1",
    "eslint": "^6.0.1",
    "stylelint": "^13.1.0"
  },
  "materialConfig": {
    "template": "@dc-tech/ice-vue-material-template",
    "type": "vue"
  },
  "iceworks": {
    "type": "vue"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MrManYoYo/dcfe-materials"
  }
}
